import Ember from 'ember';

export default Ember.Component.extend({

  // averageRating: Ember.computed('product.comments', function() {
  //   var sum = 0;
  //
  //   for(var i=0; i<product.comments.length; i++) {
  //     sum += parseFloat(product.comments[i].rating);
  //   }
  //   var answer = sum / product.comments.length;
  //   return answer;
  // });
});
