<div class="col-md-6 well" id="productWell">
  {{#link-to 'product' product.id}}<h3>{{product.name}}</h3>{{/link-to}}
  <img src="{{product.image}}">
  <h5>{{product.cost}}</h5>
  <h5>{{product.category}}</h5>
</div>
