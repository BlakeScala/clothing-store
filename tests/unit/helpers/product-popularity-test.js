
import { productPopularity } from 'clothing-store/helpers/product-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | product popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productPopularity([42]);
  assert.ok(result);
});

